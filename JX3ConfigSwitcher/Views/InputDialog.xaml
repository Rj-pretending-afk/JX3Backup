<Window x:Class="JX3ConfigSwitcher.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入"
        Width="430"
        Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        Foreground="#F5F8FF"
        FontFamily="Microsoft YaHei UI, Segoe UI, Arial">
    <Window.Resources>
        <SolidColorBrush x:Key="GlassPanelBrush" Color="#EA111824"/>
        <SolidColorBrush x:Key="GlassInputBrush" Color="#80101824"/>
        <SolidColorBrush x:Key="GlassInputHoverBrush" Color="#A91A2636"/>
        <SolidColorBrush x:Key="GlassBorderBrush" Color="#66DCE7FF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#7CC7FF"/>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#F8FBFF"/>
            <Setter Property="Background" Value="{StaticResource GlassInputBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource GlassInputHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource GlassInputBrush}"/>
            <Setter Property="Foreground" Value="#F5F8FF"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="8"
            Padding="20"
            CornerRadius="24"
            Background="{StaticResource GlassPanelBrush}"
            BorderBrush="{StaticResource GlassBorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="PromptText"
                       FontSize="14"
                       Foreground="#C8D5E8"
                       TextWrapping="Wrap"/>

            <TextBox x:Name="ValueBox"
                     Grid.Row="1"
                     Margin="0,16,0,0"
                     Height="40"
                     VerticalContentAlignment="Center"/>

            <StackPanel Grid.Row="2"
                        Margin="0,18,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="取消"
                        Width="86"
                        Margin="0,0,10,0"
                        Click="Cancel_Click"/>
                <Button Content="确定"
                        Width="86"
                        Background="#CC2F80ED"
                        BorderBrush="#AA9FD7FF"
                        Click="Ok_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
